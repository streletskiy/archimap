# Bundle

## Current state

- Bundle analysis report generated by:
  - `npm run analyze`
  - output: `frontend/build/bundle-analysis.html`

## Chunk strategy

- Vite manual chunks in `frontend/vite.config.ts`:
  - `admin`
  - `maplibre`
  - `pmtiles`
  - `markdown`
  - `vendor`

## Before/after snapshot

- Before largest client chunk: `929.62 kB`.
- After largest client chunk: `893.57 kB` (from perf smoke filesystem scan).

## Heaviest dependencies

- `maplibre-gl` (dominant map render/runtime cost)
- `pmtiles`
- Svelte runtime/vendor utilities

## Next reduction candidates

- route-level split around map toolchain adapters
- optional workerization of heavy map-side transforms
- evaluate alternative map style payload and glyph/font footprint
